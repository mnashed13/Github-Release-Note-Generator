# GitHub Personal Access Token (Required)
# Generate this token from GitHub: Settings > Developer Settings > Personal Access Tokens
GITHUB_TOKEN=your_github_token_here

# GitHub Repository Owner (Required)
# This is the username or organization name that owns the repository
GITHUB_OWNER=your_github_username_or_org

# GitHub Repository Name (Required)
# The name of the repository you want to generate release notes for
GITHUB_REPO=your_repository_name

# Optional Configuration
# Release Tag (Optional - defaults to latest)
END_VERSION=v1.1.0
START_VERSION=v2.0.0

# GitHub API Base URL (Optional - defaults to https://api.github.com)
# Only change this if you're using GitHub Enterprise
# GITHUB_API_URL=https://api.github.com

# Number of Pull Requests to fetch (Optional - defaults to 100)
# MAX_PRS=100

# Email Configuration
EMAIL_FROM=release-bot@yourdomain.com
EMAIL_TO=team@yourdomain.com
EMAIL_CC=other-team@yourdomain.com

# SMTP Configuration
SMTP_HOST=smtp.yourdomain.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# Other Configuration
# Add any other configuration variables here 